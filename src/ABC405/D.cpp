#include <bits/stdc++.h>

#include <atcoder/all>

using namespace std;
using namespace atcoder;

int main() {
  int h, w;
  cin >> h >> w;
  vector<string> s(h);
  for (auto &x : s) {
    cin >> x;
  }

  return 0;
}
