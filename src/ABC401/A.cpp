#include <bits/stdc++.h>

#include <atcoder/all>

using namespace std;
using namespace atcoder;

int main() {
  int s;
  cin >> s;
  if (200 <= s && s < 300) {
    cout << "Success" << endl;
  } else {
    cout << "Failure" << endl;
  }

  return 0;
}
