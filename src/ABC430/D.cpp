#include <bits/stdc++.h>

#include <atcoder/all>

using namespace std;
using namespace atcoder;

using ll = long long;

int main() {
  int n;
  cin >> n;
  vector<ll> x(n);
  for (int i = 0; i < n; i++) {
    cin >> x[i];
  }

  set<ll> y;
  y.insert(0);

  ll ans = 0LL;
  for (int i = 0; i < n; i++) {
    
  }

  cout << ans << endl;

  return 0;
}
